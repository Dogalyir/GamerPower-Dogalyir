services:
  telegrampower:
    build: .
    restart: always
    env_file:
      - stack.env
    environment:
      - NODE_ENV=production
    volumes:
      - power_data:/usr/src/app/power.sqlite


volumes:
  power_data:
    name: gamerpower_data